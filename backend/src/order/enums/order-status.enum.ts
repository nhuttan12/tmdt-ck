export enum OrderStatus {
  Pending = 'pending',
  Confirmed = 'confirmed',
  Processing = 'processing',
  Shipping = 'shipping',
  Delivered = 'delivered',
  Canceled = 'canceled',
  Returned = 'returned',
  Failed = 'failed',
}
